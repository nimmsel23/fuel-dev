#!/usr/bin/env python3
"""Thin entry point — Logik lebt in fuel/meal.py."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from fuel.meal import main

if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        print("\nAbbruch.")
        sys.exit(130)
