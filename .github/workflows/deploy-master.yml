# Live-Deploy für Fuel: Push auf master
# 
# Baut die App und lädt sie direkt live auf Firebase hoch.
# Analog zur Preview-Action wird das vitalos Meta-Repo ausgecheckt,
# damit alle Nachbar-Repos (cross-repo imports) vorhanden sind.

name: Live Deploy (Fuel master)

on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - 'src/**'

jobs:
  deploy_live:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout vitalos (Meta-Repo, liefert Nachbar-Repos)
        uses: actions/checkout@v4
        with:
          repository: nimmsel23/vitalos
          ref: master

      - name: Submodules via HTTPS + Token
        run: |
          git config --global url."https://x-access-token:${{ github.token }}@github.com/".insteadOf "git@github.com:"
          git submodule sync
          git submodule update --init --recursive

      - name: fuel-dev auf den ausgelösten Commit setzen
        run: |
          SHA="${{ github.sha }}"
          cd fuel-app
          git fetch origin "$SHA"
          git checkout "$SHA"

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install (Workspaces)
        run: npm ci

      - name: Firebase config bereitstellen
        run: |
          if [ -n "$FIREBASE_CONFIG" ]; then
            echo "$FIREBASE_CONFIG" > fuel-app/firebase.config.js
            echo "$FIREBASE_CONFIG" > fitness-app/firebase.config.js
            echo "$FIREBASE_CONFIG" > journal-app/firebase.config.js
          elif [ ! -f fuel-app/firebase.config.js ]; then
            cp firebase.config.js fuel-app/firebase.config.js
            cp firebase.config.js fitness-app/firebase.config.js
            cp firebase.config.js journal-app/firebase.config.js
          fi
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_CONFIG }}

      - name: Build Fitness KB data
        run: npm run build:kb-data --workspace=fitness-app

      - name: Build Fuel (Live)
        run: npm run build:firebase --workspace=fuel-app

      - name: Deploy Live
        id: firebase_deploy
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FITNESS_AOS || secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: fitness-aos
          entryPoint: fuel-app
          channelId: live

      - name: Telegram-Benachrichtigung senden
        if: success()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🚀 Fuel ist LIVE deployed (master)!

            🔗 Live-URL: https://fitness-aos.web.app
            (oder eine verknüpfte Domain)

            Commit: ${{ github.event.head_commit.message }}
