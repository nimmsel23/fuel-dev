/**
 * Firestore Data Layer — Fuel Centre (Multi-User)
 *
 * Seit 2026-07-09 nur noch Barrel: die Implementierung liegt modular in
 * ./db/firestore/ (core, utils, settings, nutrition, journal, supplements).
 * Alle bestehenden Import-Pfade auf diese Datei bleiben gültig.
 */

export * from "./db.firestore.js";
