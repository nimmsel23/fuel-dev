# Food View Layer

Dieser Ordner enthält die Komponenten der `FoodView.jsx`.
- `FoodCatalog.jsx`: Lädt den Katalog aus der Datenbank und loggt Gerichte.

Der Rezept-Builder ist ein eigener Tab: `../RecipeBuilder/RecipeBuilder.jsx` + `../RecipeBuilderView.jsx`.
