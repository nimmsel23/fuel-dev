# Micros View Layer

Dieser Ordner enthält die Komponenten der `MicrosView.jsx`.
Da der Tab bald ausgebaut wird, ist die Heatmap-Tabelle (`MicrosGrid.jsx`), die Legende (`MicrosLegend.jsx`) und die Logik (`utils.js`) hier bereits sauber getrennt und für zukünftige Widgets vorbereitet.
